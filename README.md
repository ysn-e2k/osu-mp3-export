# osu-mp3-export
Extracteur MP3 pour osu! — récupère automatiquement les musiques de tes beatmaps .osu et les exporte proprement en .mp3, avec une interface Win32 en GDI+ (background, animations GIF, tout embarqué dans l'exe).
